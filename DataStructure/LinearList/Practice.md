1. 
```
   1. false
   2. 4 
   3. a, c, exception, exception,
   4. 0, 3, -1
   5. L=(b, c, d) L=(b, c) exception
   6. L=(e, a, b, c, d), L=(e, a, f, b, c, d) L=(e, a, f, g, b, c, d)
        L=(e, a, f, g, h, b, c, d) L=(e, a, f, g, h, h, b, c, d) exception
   19.
     定理： 如果我们总是按一个乘法因子来增加数组长度，那么实施一系列线性表的操作所需要的时间与不用改变数组长度时相比，至多增加一个常数因子
     
     证明：假设数组长度增加因子为k，则从长度为1的数组开始插入，数组长度一共增加了x次
          每次数组长度增加时，数组的长度为：
          1 k k^2 ... k^x，其中k^x=n
          ∴改变数组长度的时间是θ(sigma[i=1 -> x](k^i))=θ(k^0 + k^1 + ... + k^x)=θ(k^x)=θ(n)
```